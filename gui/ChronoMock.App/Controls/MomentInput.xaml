<UserControl x:Class="ChronoMock.App.Controls.MomentInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:app="clr-namespace:ChronoMock.App">
  <!-- Shared moment input (chrono-mock 7.1): an ISO date field plus a calendar popup and an optional 24h
       time field. The value is composed culture-invariantly by MomentField, so a Polish box and a US VM read
       a typed date identically (rule 2) - that is why this is NOT a WPF DatePicker, whose text parsing follows
       the OS locale. The validation message is shown by the consumer BELOW the whole row
       (MomentField.ActiveErrorKey), never inside this control, so an error never changes the row height.
       Bound to a MomentField DataContext.
       🔴 A WrapPanel, not a horizontal stack. Given the room it lays date, calendar and time on one line and
       stays level with the buttons beside it - a horizontal parent measures it at infinite width, so it never
       wraps there. In a narrow column, the calculator's builder, the time field drops to the next line
       instead of being clipped off the right edge, which a fixed-width stack did. -->
  <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
    <!-- Wide enough for YYYY-MM-DD with room to spare. The box takes the parts library's TextBox style
         (implicit) and its placeholder, so it reads as the same field the rest of the interface uses. -->
    <TextBox Text="{Binding DateText, UpdateSourceTrigger=PropertyChanged}"
             app:PartState.Placeholder="{DynamicResource moment.date_placeholder}"
             ToolTip="{DynamicResource tip.date_field}"
             Width="{StaticResource MomentDateFieldWidth}" FontFamily="{StaticResource FontFamilyMono}"
             VerticalAlignment="Center" />

    <ToggleButton x:Name="CalendarToggle" Style="{StaticResource CalendarToggleStyle}"
                  Margin="{StaticResource SpaceXs}" VerticalAlignment="Center"
                  ToolTip="{DynamicResource action.pick_date}"
                  AutomationProperties.Name="{DynamicResource action.pick_date}" />
    <Popup IsOpen="{Binding IsChecked, ElementName=CalendarToggle}" StaysOpen="False"
           PlacementTarget="{Binding ElementName=CalendarToggle}" Placement="Bottom" AllowsTransparency="True">
      <Border Background="{DynamicResource BrushBgSurfaceAlt}"
              CornerRadius="{StaticResource RadiusSm}" Padding="{StaticResource SpaceXs}">
        <Calendar SelectedDate="{Binding SelectedDate, Mode=TwoWay}" SelectedDatesChanged="OnDatePicked" />
      </Border>
    </Popup>

    <TextBox Text="{Binding TimeText, UpdateSourceTrigger=PropertyChanged}"
             app:PartState.Placeholder="{DynamicResource moment.time_placeholder}"
             ToolTip="{DynamicResource tip.time_field}"
             Width="{StaticResource MomentTimeFieldWidth}" FontFamily="{StaticResource FontFamilyMono}"
             Margin="{StaticResource SpaceXs}" VerticalAlignment="Center" />
  </WrapPanel>
</UserControl>

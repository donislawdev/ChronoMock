<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="18.9.0" />
    <PackageReference Include="xunit.v3" Version="4.0.0" />
    <PackageReference Include="xunit.runner.visualstudio" Version="4.0.0" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\ChronoMock.Protocol\ChronoMock.Protocol.csproj" />
    <!-- Build the conformance target before the tests, but do not link it into the test assembly. -->
    <ProjectReference Include="..\ChronoMock.TestTarget\ChronoMock.TestTarget.csproj" ReferenceOutputAssembly="false" />
  </ItemGroup>

</Project>
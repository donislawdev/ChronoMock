# License gate for the product workspace (Rule 8, GPL-3.0-only).
# The tool ships GPL-3.0-only; every dependency must be permissive and GPL-3 compatible.
# Run: cargo deny check licenses
[licenses]
version = 2
confidence-threshold = 0.9
allow = [
    "GPL-3.0-only",
    "MIT",
    "Apache-2.0",
    "Unicode-3.0",
]

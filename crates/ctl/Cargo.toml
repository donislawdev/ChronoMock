[package]
name = "chrono-ctl"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
name = "chrono_ctl"

# Shared control-memory contract between the mechanism (chrono-mech) and the
# injected hook (chrono-hook). Two processes read/write the SAME #[repr(C)] layout,
# so it lives in exactly one place. This is a data contract, part of the mechanism
# layer - NOT an interface layer, and the logical core must never depend on it.
[dependencies]

[package]
name = "chrono-proto"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
name = "chrono_proto"

# The protocol layer serializes core state to the machine protocol (ADR-6, docs/08)
# and back. It is the only path in and out of the core process.
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
